## 037-The anatomy of a function

Functions

```py

def performOperation(num1, num2, operation):
    if operation == 'sum':
        return num1 + num2
    if operation == 'multiply':
        return num1 * num2
    
performOperation(2, 3, 'sum')

# 5
```

Named Parameters

```py
def performOperation(num1, num2, operation='sum'):
    if operation == 'sum':
        return num1 + num2
    if operation == 'multiply':
        return num1 * num2
    
performOperation(2, 3)

# 5

def performOperation(num1, num2, operation='sum', message='Default message'):
    print(message)
    if operation == 'sum':
        return num1 + num2
    if operation == 'multiply':
        return num1 * num2
    
performOperation(2, 3, message='A new message!', operation='multiply')

# A new message!
# 6
```

*args

```py

def performOperation(args):
    print(args)
    
performOperation(1,2,3)

# TypeError

def performOperation(*args):
    print(args)
    
performOperation(1,2,3)

# 1,2,3

performOperation(1,2,3, operation='sum')

# TypeError 
```

**kwargs

```py
def performOperation(*args, **kwargs):
    print(args)
    print(kwargs)
performOperation(1,2,3, operation='sum')

# (1, 2, 3) - tuple кортеж
# {'operation': 'sum'} - словарь dictionary


import math

def performOperation(*args, operation='sum'):
    if operation == 'sum':
        return sum(args)
    if operation == 'multiply':
        return math.prod(args)
    
performOperation(1,2,3,6,7,8, operation='sum')

# 27

```

## 038-Variables and scope

 Видимость в простой функции
```py
def performOperation(*args, **kwargs):
    print(args)
    print(kwargs)

performOperation(1, 2, operation='sum')
# (1, 2) - кортеж
# {'operation': 'sum'} - словарь
```

Locals ()
```py
# Получить словарь всех переменных, которые были переданы

def performOperation(num1, num2, operation='sum'):
    print(locals())
    
performOperation(1, 2, operation='multiply')
# {'num1': 1, 'num2': 2, 'operation': 'multiply'}

```

globals()
```py
globals()
# будет выведен список глобальных переменных
```

Global and Local scope
```py
message = 'Some global data'

def function1(varA, varB):
    print(message)
    print(locals())
    
    
def function2(varC, varB):
    print(message)
    print(locals())
    
function1(1, 2)
function2(3, 4)

# Some global data
# {'varA': 1, 'varB': 2}
# Some global data
# {'varC': 3, 'varB': 4}

```

```py
message = 'Some global data'
varA = 2
def function1(varA, varB):
    message = 'Some local data'
    print(varA)
    print(message)
    print(locals())    
    
def function2(varC, varB):
    print(varA)
    print(message)
    print(locals())
    
function1(1, 2)
function2(3, 4)

# 1 - varA из локальной области, из функции
# Some local data - message из локальной области, из функции
# {'varA': 1, 'varB': 2, 'message': 'Some local data'}
# 2 - varA из глобальной области, вне функций
# Some global data - message из глобальной области, вне функций
# {'varC': 3, 'varB': 4}

```

Функция внутри другой функции
```py
def function1(varA, varB):
    message = 'Some local data'
    print(varA)
    def inner_function(varA, varB):
        print(f'inner_function local scope: {locals()}')
        
    inner_function(123, 456)
    

function1(1, 2) 

# 1
# inner_function local scope: {'varA': 123, 'varB': 456}

```

```py
def function1(varA, varB):
    message = 'Some local data'
    print(varA)
    def inner_function(varA, varB):
        print(f'inner_function local scope: {locals()}')
    
    print(locals())
    inner_function(123, 456)
    

function1(1, 2) 

# 1
# {'varA': 1, 'varB': 2, 'message': 'Some local data', 'inner_function': <function function1.<locals>.inner_function at 0x107b6af80>}
# inner_function local scope: {'varA': 123, 'varB': 456}

```

##  039-Functions as variables

Variables as Functions

```py
x = 5

def x():
    return 5
```

Viewing function data with __code__

```py
print(x.__code__.co_varnames)
print(x.__code__.co_code)

# ()
# b'd\x01S\x00'
```

Text processing in Python

```py
text = '''
Beautiful is better than ugly.
Explicit is better than implicit.
Simple is better than complex.
Complex is better than complicated.
Flat is better than nested.
Sparse is better than dense.
Readability counts.
Special cases aren't special enough to break the rules.
Although practicality beats purity.
Errors should never pass silently.
Unless explicitly silenced.
In the face of ambiguity, refuse the temptation to guess.
There should be one-- and preferably only one --obvious way to do it.
Although that way may not be obvious at first unless you're Dutch.
Now is better than never.
Although never is often better than *right* now.
If the implementation is hard to explain, it's a bad idea.
If the implementation is easy to explain, it may be a good idea.
Namespaces are one honking great idea -- let's do more of those!
'''
```

```py
def lowercase(text):
    return text.lower()

def removePunctuation(text):
    punctuations = ['.', '-', ',', '*']
    for punctuation in punctuations:
        text = text.replace(punctuation, '')
    return text

def removeNewlines(text):
    text = text.replace('\n', ' ')
    return text

def removeShortWords(text):
    return ' '.join([word for word in text.split() if len(word) > 3])

def removeLongWords(text):
    return ' '.join([word for word in text.split() if len(word) < 6])
```

```py
processingFunctions = [lowercase, removePunctuation, removeNewlines, removeLongWords]

for func in processingFunctions:
    text = func(text)

print(text)

# is than ugly is than is than is than flat is than is than dense cases to break the rules beats never pass in the face of the to guess there be one and only one way to do it that way may not be at first dutch now is than never never is often than right now if the is hard to it's a bad idea if the is easy to it may be a good idea are one great idea let's do more of
```

Lambda functions

```py
2 + 3
# 5
```

Лямбда - функция, у которой нет имени. Лямбда выполняется в одну строку и не имеет многострочности. 

```py
(lambda x: x + 3)(5)
# 8
```

То же самое, что 

```py
def name(x):
    return x + 3
name(5)
# 8
```

```py
myList = [5,4,3,2]
sorted(myList)

# [2, 3, 4, 5]
```

Сортировка не чисел с помощью лямбда.
```py
# Список словарей
myList = [{'num': 3}, {'num': 2}, {'num': 1}]
sorted(myList, key=lambda x: x['num'])

# [{'num': 1}, {'num': 2}, {'num': 3}]
```

---
