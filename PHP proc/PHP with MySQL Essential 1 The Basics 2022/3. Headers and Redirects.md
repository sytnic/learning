## 017-Output buffering

Старт и конец буферизации вывода:

    ob_start()

    ob_end_flush()

`ob_end_flush()` вызывается автоматически при завершении php-кода, поэтому можно не вызывать вручную. 

---