INCREMENT / DECREMENT
```
a=a+ 1;
a += 1;
at++;

++a;
```

INCREMENT / DECREMENT
```
a = a + 1; 
a += 1;
a++;
++a;
```

INCREMENT / DECREMENT 
```
a = a + 1; a - a - 1; a += 1; a -- 1; a++; a-0 1 ++a; --a; 
```

INCREMENT / DECREMENT
```
   a = a + 1; a = a - 1;
   a += 1;      a -= 1;
   a++;           a--
   ++a;           --a;
```



